capsule {
  id (playground.ace)
  version (0.1.0)
  format (3)
  targets {
    target (bixby-mobile-en-US)
  }

  capsule-imports {
    
    import (viv.geo) {
      as (geo) version (9.6.3)
    }
    import (viv.core) {
      as (core)
    }   
    import (viv.time) {
      as (time) version (2.63.9)
    }
  }
}

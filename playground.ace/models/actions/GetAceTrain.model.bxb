action (GetAceTrain) {
  type(Search)
  description (__DESCRIPTION__)

  collect {
    input (scheduleNumber) {
      type (ScheduleNumber)
      min (Optional) max (One)
    }
  }
  output (ACETrain)
}

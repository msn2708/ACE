layout-macro-def (train-summary) {
  match: Vehicle (train)
  mode (Summary)

  content {
    section {
      content {
        paragraph {
          value("#{value(train.scheduleNumber)}")
          value("#{value(train.onSchedule)}")
          style(Detail_M)
        }
      }
    }
  }
}

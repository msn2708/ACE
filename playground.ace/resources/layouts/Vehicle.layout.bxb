layout-macro-def (train-summary) {
  match: Vehicle (this)
  mode (Summary)

  content {
    section {
      content {
        paragraph {
          value(this)
          if(exists(value(this.minutesToNextStops))) {
            value(this.minutesToNextStops)
          }
        }
      }
    }
  }
}

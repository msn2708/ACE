layout-macro-def (train-summary) {
  params {
    param (train) {
      type (Vehicle)
      min (Required) max (One)
    }
  }
  content {
    section {
      content {
        paragraph {
          value("#{value(train.scheduleNumber)}")
        }
      }
    }
  }
}
